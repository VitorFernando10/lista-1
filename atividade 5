algoritimo( media da nota)
 real: nota1, nota2, midia

   escreva("Nota 1: ")
   leia(nota1)
   escreva("Nota 2: ")
   leia(nota2)
   midia = (nota1 + nota2) / 2
   escreva("A média entre " + nota1 + " e " + nota2 + " é igual a "+ media)
